# Mesh-wide STRICT mTLS: plaintext traffic to sidecar-injected pods is rejected.
# kubectl apply -f istio-mtls.yaml
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
  namespace: istio-system
spec:
  mtls:
    mode: STRICT
