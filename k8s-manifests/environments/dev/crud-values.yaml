nameOverride: crud-api

image:
  repository: crud-api
  tag: dev

env:
  DB_HOST: postgres.default.svc.cluster.local
  DB_PORT: "5432"
  DB_NAME: crud
  # Credentials belong in a Secret, not here.

autoscaling:
  minReplicas: 1
  maxReplicas: 3
