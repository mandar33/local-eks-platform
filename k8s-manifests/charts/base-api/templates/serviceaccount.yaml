apiVersion: v1
kind: ServiceAccount
metadata:
  # One identity per app, so Istio AuthorizationPolicies can tell callers apart
  # (principal: cluster.local/ns/default/sa/<nameOverride>)
  name: {{ .Values.nameOverride }}
  namespace: default
